import React from "react";
import { Outlet } from "react-router-dom";

const DashboardSettingsIndex = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default DashboardSettingsIndex;
